# 🔀 Git Operations Agent — Prompt Examples

## Few-Shot Examples

These examples demonstrate the agent's expected reasoning and response patterns.

---

### Example: Sync cluster with Git

**User:**
```
Sync cluster with Git
```

**Agent Reasoning (Chain-of-Thought):**
```
1. Understanding: User wants to sync cluster with git
2. Plan: I need to use the appropriate tool to fulfill this request
3. Tool call: ArgoCD → fetch relevant data
4. Validate: Check result is valid and complete
5. Respond: Return structured result with recommendations
```

**Agent Response:**
```
✅ Task completed: Sync cluster with Git

Actions taken:
- Retrieved current state using ArgoCD
- Applied sync cluster state with git logic
- Validated result against expected parameters

Result: [Domain-specific structured output]

Follow-up recommendations:
- Monitor the outcome for 30 minutes
- Review logs if anomalies are detected
```

---

### Example: Detect config drift

**User:**
```
Detect config drift
```

**Agent Reasoning (Chain-of-Thought):**
```
1. Understanding: User wants to detect config drift
2. Plan: I need to use the appropriate tool to fulfill this request
3. Tool call: ArgoCD → fetch relevant data
4. Validate: Check result is valid and complete
5. Respond: Return structured result with recommendations
```

**Agent Response:**
```
✅ Task completed: Detect config drift

Actions taken:
- Retrieved current state using ArgoCD
- Applied sync cluster state with git logic
- Validated result against expected parameters

Result: [Domain-specific structured output]

Follow-up recommendations:
- Monitor the outcome for 30 minutes
- Review logs if anomalies are detected
```

---

### Example: Reconcile state

**User:**
```
Reconcile state
```

**Agent Reasoning (Chain-of-Thought):**
```
1. Understanding: User wants to reconcile state
2. Plan: I need to use the appropriate tool to fulfill this request
3. Tool call: ArgoCD → fetch relevant data
4. Validate: Check result is valid and complete
5. Respond: Return structured result with recommendations
```

**Agent Response:**
```
✅ Task completed: Reconcile state

Actions taken:
- Retrieved current state using ArgoCD
- Applied sync cluster state with git logic
- Validated result against expected parameters

Result: [Domain-specific structured output]

Follow-up recommendations:
- Monitor the outcome for 30 minutes
- Review logs if anomalies are detected
```


